name: scanner_app
description: DocScan - Fast & Smart Scanner
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5+9
  path_provider: ^2.1.3
  pdf: ^3.10.8
  share_plus: ^9.0.0
  permission_handler: ^11.3.1
  google_mlkit_document_scanner: ^0.1.0
  image: ^4.2.0
  web: ^0.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
